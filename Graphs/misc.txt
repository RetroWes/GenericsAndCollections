    graph.startingNode = graph.new Node(1);
    graph.startingNode.addNode(2);
    System.out.println(graph.startingNode.adjacents.get(0).value);
    graph.startingNode.adjacents.get(0).addNode(3);
    System.out.println(graph.startingNode.adjacents.get(0).adjacents.get(1).value);

        // for(int i = 1; i < 6; i++)  {startingNode.addNode(i);}
    // startingNode.adjacents.get(3).addNode(5);
    // startingNode.adjacents.get(4).addEdge(startingNode.adjacents.get(3).adjacents.get(0));
    // startingNode.addNode(6);
    // startingNode.adjacents.get(startingNode.adjacents.size() - 1).addNode(7);
    // startingNode.adjacents.get(startingNode.adjacents.size() - 1).addNode(8);